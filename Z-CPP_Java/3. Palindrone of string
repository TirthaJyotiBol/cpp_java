PLAINDRONE :- 
if string == string_reverse then we call this as ppalindrone
e.g. abbd , aca, PCP, ANANA



#include <bits/stdc++.h>
using namespace std;

int main() {
   string str;
   cin>>str;
   string rev_str;
   for(int i=str.size()-1;i>=0;i--){
      rev_str.push_back(str[i]);
   }
  if(rev_str==str){
      cout<<"Yes";
  }
  else{
      cout<<"No";
  }
}

